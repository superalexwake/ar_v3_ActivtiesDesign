# 站点名：巴基斯坦-92PAK

## 版型: 电子首页 — 暗色青绿

### 构建指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar086 npx vite build --mode sit

### 构建指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar086 npx vite build

### 启动指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar086 npx vite --mode sit

### 启动指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar086 npx vite