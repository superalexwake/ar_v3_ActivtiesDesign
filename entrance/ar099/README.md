# 站点名：巴基斯坦 ar099-92.Game

## 版型: 公版 3 - 青绿（public3Home + public3Style）

### 构建指令 sit:

BRANCH_NAM=$V NODE_ENV=ar099 npx vite build --mode sit

### 构建指令 prd:

BRANCH_NAM=$V NODE_ENV=ar099 npx vite build

### 启动指令 sit:

BRANCH_NAM=$V NODE_ENV=ar099 npx vite --mode sit

### 启动指令 prd:

BRANCH_NAM=$V NODE_ENV=ar099 npx vite
