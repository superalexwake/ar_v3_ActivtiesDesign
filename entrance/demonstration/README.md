# 站点名：新加坡-演示

## 版型: 红1首页 — 通用内页

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=demonstration npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=demonstration npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=demonstration npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=demonstration npx vite