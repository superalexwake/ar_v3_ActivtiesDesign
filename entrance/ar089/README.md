# 站点名：ar089-92paisa

## 版型: 公版3-深色2（樱花粉）

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar089 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar089 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar089 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar089 npx vite

