# 站点名：巴基斯坦ar092-YAY.WIN

## 版型: 公版6 — 白绿色

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar092 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar092 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar092 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar092 npx vite