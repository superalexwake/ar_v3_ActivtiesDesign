# 站点名：巴基斯坦 ar100-Pakvip

## 版型: 深色 1（荧光绿)

### 构建指令 sit:

BRANCH_NAM=$V NODE_ENV=ar100 npx vite build --mode sit

### 构建指令 prd:

BRANCH_NAM=$V NODE_ENV=ar100 npx vite build

### 启动指令 sit:

BRANCH_NAM=$V NODE_ENV=ar100 npx vite --mode sit

### 启动指令 prd:

BRANCH_NAM=$V NODE_ENV=ar100 npx vite
