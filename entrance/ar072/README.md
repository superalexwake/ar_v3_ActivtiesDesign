# 站点名：ar072

## 版型: 深色2（渐变黄）

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar072 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar072 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar072 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar072 npx vite

