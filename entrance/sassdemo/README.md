# 站点名：浅色3（橄榄绿）

## 版型: sassdemo  sass线上测试站点

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=sassdemo npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=sassdemo npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=sassdemo npx vite --mode sit


### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=sassdemo npx vite

