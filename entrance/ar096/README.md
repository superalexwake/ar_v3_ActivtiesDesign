# 站点名：印度 ar096-RAXI.WIN

## 版型: 电子首页 - 黑红色

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar096 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar096 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar096 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar096 npx vite