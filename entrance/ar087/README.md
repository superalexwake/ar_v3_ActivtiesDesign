# 站点名：92glory-ar087

## 版型: 浅色1  公版3-深沉蓝

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar087 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar087 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar087 npx vite --mode sit


### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar087 npx vite

