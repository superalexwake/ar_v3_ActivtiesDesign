# 站点名：ar079 tashanwin

## 版型: public5

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar079 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar079 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar079 npx vite --mode sit


### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar079 npx vite

