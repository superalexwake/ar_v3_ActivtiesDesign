# 站点名：ar090

## 版型: 深色3（神秘紫）

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar090 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar090 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar090 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar090 npx vite

