# 站点名：ar069

## 版型: 深色1（荧光绿)

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar069 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar069 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar069 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar069 npx vite

