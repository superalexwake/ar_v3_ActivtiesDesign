# 站点名：oknew

## 版型: public3

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=oknew npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=oknew npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=oknew npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=oknew npx vite

