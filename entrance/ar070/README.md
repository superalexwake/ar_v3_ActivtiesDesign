# 站点名：印度-dogeclub

## 版型: 电子首页 — 红色内页

### 构建指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar070 npx vite build --mode sit

### 构建指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar070 npx vite build

### 启动指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar070 npx vite --mode sit

### 启动指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar070 npx vite