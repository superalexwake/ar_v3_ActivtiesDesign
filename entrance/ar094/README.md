# 站点名：巴基斯坦ar094-POPZAR

## 版型: 公版8 - 白蓝 

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar094 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar094 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar094 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar094 npx vite