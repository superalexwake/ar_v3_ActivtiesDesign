# 站点名：Fantasy Gems ar071

## 版型: 电子版面 — 翡翠绿

### 构建指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar071 npx vite build --mode sit

### 构建指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar071 npx vite build

### 启动指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar071 npx vite --mode sit

### 启动指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar071 npx vite