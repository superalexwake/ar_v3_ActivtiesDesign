# 站点名：ar078

## 版型: 公版3 白金色

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar078 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar078 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar078 npx vite --mode sit


### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar078 npx vite

