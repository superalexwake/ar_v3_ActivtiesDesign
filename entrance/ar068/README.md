# 站点名：巴基斯坦-92jeet

## 版型: goGame

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar068 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar068 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar068 npx vite --mode sit


### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar068 npx vite

