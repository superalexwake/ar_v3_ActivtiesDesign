# 站点名：777BIGWINGAME-ar088

## 版型: 浅色  公版5-橄榄绿

### 构建指令sit:
BRANCH_NAM=$V NODE_ENV=ar088 npx vite build --mode sit

### 构建指令prd:
BRANCH_NAM=$V NODE_ENV=ar088 npx vite build

### 启动指令sit:
BRANCH_NAM=$V NODE_ENV=ar088 npx vite --mode sit

### 启动指令prd:
BRANCH_NAM=$V NODE_ENV=ar088 npx vite

