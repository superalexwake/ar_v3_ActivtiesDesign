# 站点名：pop888

## 版型: 电子(黑色) — 黑色内页

### 构建指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar038 npx vite build --mode sit

### 构建指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar038 npx vite build

### 启动指令sit:
cross-env BRANCH_NAM=$V NODE_ENV=ar038 npx vite --mode sit

### 启动指令prd:
cross-env BRANCH_NAM=$V NODE_ENV=ar038 npx vite